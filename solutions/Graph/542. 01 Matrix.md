
## BFS 與 Queue 的關係核心機制：FIFO (First In, First Out)

- 搜尋順序：按「層」擴散。先處理距離起點為 $k$ 的所有節點，再處理 $k+1$ 的節點。
- 適用場景：
  - 非加權圖的最短路徑。
  - 層序遍歷（Level Order Traversal）。
- 關鍵操作：
  - popleft(): 取出當前層的節點。
  - append(): 將下一層的節點加入隊伍。
